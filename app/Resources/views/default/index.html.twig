{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <a class="navbar-brand" href="#home">Terminarz</a>
                <button class="navbar-toggler" type="button" data-target="#hiddenMenu" data-toggle="collapse" aria-controls="hiddenMenu" aria-expanded="false" aria-label="Nawigacja">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="hiddenMenu">
                    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                        <li class="nav-item">
                            <a href="{{ path('fos_user_security_login') }}" class="nav-link">Login</a>
                        </li>
                        <li>
                            <a href="{{ path('fos_user_registration_register') }}" class="nav-link">Rejestracja</a>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
        <div class="baner">


        </div>
    </div>
{% endblock %}


